name: 'Pebble'
on:
  - workflow_dispatch
jobs:
  test:
    uses: ./.github/workflows/main.yml
    with:
      mode: '["build"]'
      alphas: '["qwerty"]'
      kmonad_os: '["mac"]'
      mapping: '["kinesis_advantage"]'
